---
- name: "Rule13"
  hosts: all
  sources:
    - ansible.eda.range:
        limit: 10
        batch_count: 5
        delay: 1
  rules:
    - name: event is defined
      condition: event.body is defined
      action:
        run_job_template:
          name: "run_basic"
          organization: Default
