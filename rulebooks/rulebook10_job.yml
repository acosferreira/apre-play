---
- name: "Rule10"
  hosts: all
  sources:
    - ansible.eda.range:
        limit: 60
        delay: 1  # 60 seconds
  rules:
    - name: Rule 10 Job event
      condition: event.payload.name == "rule10"
      action:
        run_job_template:
          name: "Job10"
