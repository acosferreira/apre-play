---
- name: "Rule7"
  hosts: all
  sources:
    - ansible.eda.range:
        limit: 10
        batch_count: 5
        delay: 1
  rules:
    - name: Rule 7 Job event
      condition: event.payload.name == "rule7"
      action:
        run_job_template:
          name: "Job7"
          organization: Default
