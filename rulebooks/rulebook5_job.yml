---
- name: "Rule5"
  hosts: all
  sources:
    - ansible.eda.range:
        limit: 60
        delay: 1  # 60 seconds
  rules:
    - name: Rule 5 Job event
      condition: event.payload.name == "rule5"
      action:
        run_job_template:
          name: "Job5"
