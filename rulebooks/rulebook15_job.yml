---
- name: "Rule15"
  hosts: all
  sources:
    - ansible.eda.range:
        limit: 60
        delay: 1  # 60 seconds
  rules:
    - name: Rule 15 Job event
      condition: event.payload.name == "rule15"
      action:
        run_job_template:
          name: "Job15"
          organization: Default
