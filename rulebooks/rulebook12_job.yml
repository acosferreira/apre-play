---
- name: "Rule12"
  hosts: all
  sources:
    - ansible.eda.range:
        limit: 10
        batch_count: 5
        delay: 1
  rules:
    - name: Rule 12 Job event
      condition: event.payload.name == "rule12"
      action:
        run_job_template:
          name: "Job12"
          organization: Default
